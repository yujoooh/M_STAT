import os
flist = os.listdir()

for fname in flist:
    if ".txt" in fname:
        if "Gageocho" in fname:
            newfname = "신안가거초(기).txt"
            os.rename(fname,newfname)
        elif "Socheongcho" in fname:
            newfname = "옹진소청초(기).txt"
            os.rename(fname,newfname)
        elif "Jeju" in fname:
            newfname = "남해동부(부).txt"
            os.rename(fname,newfname)
        elif "KoreaStrait" in fname:
            newfname = "대한해협(부).txt"
            os.rename(fname,newfname)
        elif "DaecheonBeach" in fname:
            newfname = "대천해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "JungmunBeach" in fname:
            newfname = "중문해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "HaeundaeBeach" in fname:
            newfname = "해운대해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "SongjeongBeach" in fname:
            newfname = "송정해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "GyeongpodaeBeach" in fname:
            newfname = "경포대해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "NacksanBeach" in fname:
            newfname = "낙산해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "IncheonHang" in fname:
            newfname = "인천항(부).txt"
            os.rename(fname,newfname)
        elif "PyeongtaekDangjinHang" in fname:
            newfname = "평택당진항(부).txt"
            os.rename(fname,newfname)
        elif "TaeanHang" in fname:
            newfname = "태안항(부).txt"
            os.rename(fname,newfname)
        elif "Sangwangdeungdo" in fname:
            newfname = "상왕등도(부).txt"
            os.rename(fname,newfname)
        elif "GamcheonHang" in fname:
            newfname = "감천항(부).txt"
            os.rename(fname,newfname)
        elif "남해동부" in fname:
            newfname = "남해동부(부).txt"
            os.rename(fname,newfname)
        elif "대한해협" in fname:
            newfname = "대한해협(부).txt"
            os.rename(fname,newfname)
        elif "감천항" in fname:
            newfname = "감천항(부).txt"
            os.rename(fname,newfname)
        elif "경포대해수욕장" in fname:
            newfname = "경포대해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "낙산해수욕장" in fname:
            newfname = "낙산해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "대천해수욕장" in fname:
            newfname = "대천해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "상왕등도" in fname:
            newfname = "상왕등도(부).txt"
            os.rename(fname,newfname)
        elif "송정해수욕장" in fname:
            newfname = "송정해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "인천항" in fname:
            newfname = "인천항(부).txt"
            os.rename(fname,newfname)
        elif "중문해수욕장" in fname:
            newfname = "중문해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "태안항" in fname:
            newfname = "태안항(부).txt"
            os.rename(fname,newfname)
        elif "평택당진항" in fname:
            newfname = "평택당진항(부).txt"
            os.rename(fname,newfname)
        elif "해운대해수욕장" in fname:
            newfname = "해운대해수욕장(부).txt"
            os.rename(fname,newfname)
        elif "우이도" in fname:
            newfname = "우이도(부).txt"
            os.rename(fname,newfname)
        elif "신안가거초" in fname :
            newfname = "신안가거초(기).txt"
            os.rename(fname,newfname)            
        elif "신안가거초" in fname :
            newfname = "신안가거초(기).txt"
            os.rename(fname,newfname)
        elif "옹진소청초" in fname :
            newfname = "옹진소청초(기).txt"
            os.rename(fname,newfname)
        elif "이어도" in fname :
            newfname = "이어도(기).txt"
            os.rename(fname,newfname)            
        elif "Ieodo" in fname :
            newfname = "이어도(기).txt"
            os.rename(fname,newfname)       
        elif "부산항" in fname :
            newfname = "부산항(부).txt"
            os.rename(fname,newfname)                   
        else:
            pass